package com.pmfms.enums;

/** Work Order lifecycle - BRD 14.1. */
public enum WorkOrderStatus {
    NEW,
    ASSIGNED,
    ACKNOWLEDGED,
    IN_PROGRESS,
    ON_HOLD,
    RESOLVED,
    CLOSED,
    REOPENED
}
