package com.pmfms.enums;

/** System roles as per BRD Section 13 - Roles & Permissions Matrix. */
public enum Role {
    ADMIN,
    PROPERTY_MANAGER,
    FACILITY_MANAGER,
    TENANT,
    OWNER,
    VENDOR
}
